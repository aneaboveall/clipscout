# Server-only provider credentials. At least one is required for live results.
PEXELS_API_KEY=
PIXABAY_API_KEY=

# Optional canonical URL for social metadata in production.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
